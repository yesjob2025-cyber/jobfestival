# ─────────────────────────────────────────────────────────────
# 2026 김해 JOB FESTIVAL (jobfestival.co.kr) 환경변수
# 값이 없어도 로컬 JSON 스토어 + 기본값으로 바로 구동됩니다.
# ─────────────────────────────────────────────────────────────

# 세션 쿠키 서명 키 (32자 이상 임의 문자열) — 운영 시 반드시 설정
SESSION_SECRET=""

# 현장 부스 운영자 공용 체크인 PIN (기본값 2026 — 운영 전 반드시 변경)
FEST_STAFF_PIN="2026"

# 운영 관리자 비밀번호 (/admin — 기본값 kimhae2026!)
FEST_ADMIN_PASSWORD=""

# ── Supabase (운영 데이터 영속화) ────────────────────────────
# 두 값이 있으면 Supabase, 없으면 로컬 JSON 스토어로 동작합니다.
SUPABASE_URL=""
SUPABASE_SERVICE_ROLE_KEY=""   # Project Settings → API → service_role

# 부스 시드 엔드포인트 보호용 (/api/seed?secret=...)
SEED_SECRET=""
